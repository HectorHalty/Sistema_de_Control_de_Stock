# Copiar a .env.production y completar ANTES de desplegar.
#   cp .env.production.example .env.production

# --- Dominios (usados por Caddy y builds) ---
ADMIN_DOMAIN=admin.lachacra.com
API_DOMAIN=api.lachacra.com

# --- PostgreSQL (evitar / @ : en POSTGRES_PASSWORD — rompe DATABASE_URL) ---
POSTGRES_USER=lch
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=lch_stock

# --- Auth (generar: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))") ---
JWT_SECRET=change-me-generate-with-openssl-rand-base64-48
JWT_EXPIRES_IN=1h

# Orígenes CORS: web admin + Capacitor Android (APK)
ALLOWED_ORIGINS=https://admin.lachacra.com,https://localhost,capacitor://localhost

# --- Redis ---
REDIS_PASSWORD=change-me-redis-password

# --- MinIO (NO usar minio_admin en producción) ---
MINIO_ACCESS_KEY=change-me-minio-user
MINIO_SECRET_KEY=change-me-minio-secret

# URL pública de la API (build del admin web y APK release)
VITE_API_URL=https://api.lachacra.com

# Puertos locales (solo localhost; Caddy expone HTTPS al exterior)
API_PORT=3001
ADMIN_PORT=8080
